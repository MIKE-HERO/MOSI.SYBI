<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de almacenamiento e instalación -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REBOOT" tools:ignore="ProtectedPermissions" />

    <!-- Permiso para leer logcat (Diagnóstico) -->
    <uses-permission android:name="android.permission.READ_LOGS" />

    <!-- Permisos USB -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />

    <!-- Permisos para WebView y videollamada -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Para Android 10+ (se ignora en 7.1.2 pero no molesta) -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Consultas para detectar navegadores (Firefox / Chrome) en CustomTabs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <package android:name="org.mozilla.firefox" />
        <package android:name="org.mozilla.firefox_beta" />
        <package android:name="org.mozilla.fenix" />
        <package android:name="com.android.chrome" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiosco"
        android:usesCleartextTraffic="true"
        tools:targetApi="25">

        <meta-data
            android:name="androidx.camera.core.impl.CameraValidator"
            android:value="true" />

        <!-- Pantalla principal -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Quiosco">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Todas las Activities internas -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".PhoneLoginActivity" android:exported="false" />
        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".MeasurementActivity" android:exported="false" />
        <activity android:name=".ResultsActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".BasicSettingsActivity" android:exported="false" />
        <activity android:name=".TestElementsActivity" android:exported="false" />
        <activity android:name=".LogoSettingsActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".UserSettingsActivity" android:exported="false" />
        <activity android:name=".TelemedicineSettingsActivity" android:exported="false" />
        <activity android:name=".PatientTableActivity" android:exported="false" />
        <activity android:name=".ResultTableActivity" android:exported="false" />

        <!-- Reconocimiento facial -->
        <activity android:name=".FaceRegistrationActivity" android:exported="false" />
        <activity android:name=".FaceLoginActivity" android:exported="false" />

        <!-- Info del dispositivo -->
        <activity android:name=".DeviceInfoActivity" android:exported="false" />

        <!-- Control remoto de VideoLoop (pantalla secundaria) -->
        <activity android:name=".VideoLoopControlActivity" android:exported="false" />
        <activity android:name=".PasswordSettingsActivity" android:exported="false" />

        <activity
            android:name=".CalibTestActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

        <activity
            android:name=".CalibrateActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

        <!-- TelemedicineActivity -->
        <activity
            android:name=".TelemedicineActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:exported="false" />

        <!-- Servicio -->
        <service
            android:name=".SerialService"
            android:exported="false" />

        <!-- 🔥 FileProvider (NECESARIO en Android 7+) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>